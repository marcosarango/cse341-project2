// const express = require('express').Router();
// const getInstructorsController = require('../controller/instructors')
// // const validation = require('../middleware/validate')
// const authenticate = require('../middleware/authenticate')

// //const router = express.Router()

// router.get('/', getInstructorsController.getAllCourses)
// router.get('/:id', companiesController.getCourse)
// router.post(
//   '/', authenticate.isAuthenticated, getInstructorsController,postCourse);
  
// router.put(
//   '/:id', authenticate.isAuthenticated, getInstructorsController.putCourse);
// router.delete(
//   '/:id',
//   authenticate.isAuthenticated, getInstructorsController.deleteCourse);

// module.exports = router;